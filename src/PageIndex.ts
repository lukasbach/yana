export enum PageIndex {
  Home = '__home',
  StarredItems = '__starred_items',
  FileItems = '__file_items',
  DraftItems = '__draft_items',
  AllItems = '__all_items',
  Settings = '__settings',
  Search = '__search'
}